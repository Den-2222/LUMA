<section class="section">
  <div class="page-width page-width--narrow">
    <header class="section-header text-center" data-reveal-group>
      <h1 data-reveal>{{ page.title }}</h1>
    </header>
    <div class="rte" data-reveal>{{ page.content }}</div>
  </div>
</section>

{% schema %}
{ "name": "Page" }
{% endschema %}
